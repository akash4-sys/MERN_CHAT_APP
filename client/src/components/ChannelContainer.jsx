// There is no difference between js and jsx file it just a matter of preference

import React from 'react';

const ChannelContainer = () => {
    return (
        <div>
            Channel Container
        </div>
    )
};

export default ChannelContainer;
